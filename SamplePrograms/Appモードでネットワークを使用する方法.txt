◆「現在時間を取得する」はWi-Fiへの接続が必要です◆

Blockly から Python に切り替え、以下のコードを先頭の行に書き加えてください。


#Wi-Fiに接続する
import wifiCfg
if (wifiCfg.wlan_sta.isconnected()) == False:
  wifiCfg.autoConnect(lcdShow=False)
  while(wifiCfg.wlan_sta.isconnected() == False):
      wait_ms(1)
  lcd.clear()